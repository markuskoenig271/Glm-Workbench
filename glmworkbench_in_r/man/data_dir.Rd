% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_datasets.R
\name{data_dir}
\alias{data_dir}
\title{Where the parquet files live}
\usage{
data_dir()
}
\value{
A directory path (not guaranteed to exist).
}
\description{
Resolution order: the golem option \code{data_dir} (set by \code{\link[=run_app]{run_app()}}'s
\code{data_dir} argument, available while the app runs), the
\code{GLM_WORKBENCH_DATA_DIR} environment variable (set by the Electron desktop
shell to its bundled copy), \code{data_dir} in \code{inst/golem-config.yml} for the
active \code{GOLEM_CONFIG_ACTIVE} environment, the package's own \code{inst/extdata}
if it contains parquet files, and finally \code{../data/raw} relative to the
working directory -- the Python repo layout when the RStudio project is
open.
}
